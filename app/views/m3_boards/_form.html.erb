<%= form_for(@m3_board) do |f| %>
  <% if @m3_board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@m3_board.errors.count, "error") %> prohibited this m3_board from being saved:</h2>

      <ul>
      <% @m3_board.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :data %><br>
    <%= f.text_area :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
